/**
 * tdesign v1.19.2
 * (c) 2026 tdesign
 * @license MIT
 */

'use strict';

var isObjectLike = require('./dep-22dc294c.js');
var isArray = require('./dep-4cb26289.js');

/** `Object#toString` result references. */
var stringTag = '[object String]';

/**
 * Checks if `value` is classified as a `String` primitive or object.
 *
 * @static
 * @since 0.1.0
 * @memberOf _
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a string, else `false`.
 * @example
 *
 * _.isString('abc');
 * // => true
 *
 * _.isString(1);
 * // => false
 */
function isString(value) {
  return typeof value == 'string' || !isArray.isArray(value) && isObjectLike.isObjectLike(value) && isObjectLike.baseGetTag(value) == stringTag;
}

exports.isString = isString;
//# sourceMappingURL=dep-631750cd.js.map
